# Local development: use a Neon dev branch (see CLAUDE.md "Local Development Setup")
# Production: set via GitHub Secrets / Cloud Run env vars (Neon main branch)
DATABASE_URL=postgresql://neondb_owner:PASSWORD@ep-BRANCH-ENDPOINT.us-east-2.aws.neon.tech/neondb?sslmode=require
PORT=3001

# Comma-separated list of allowed origins for CORS (e.g. http://localhost:3000,https://myapp.com)
# In development: if omitted, a small localhost allowlist is used (3000, 3001, 5173, 5174, 8080)
# In production (NODE_ENV=production): if omitted, all origins are blocked
ALLOWED_ORIGINS=

# WorkOS OAuth 2.1 SSO (required for multi-user auth)
WORKOS_API_KEY=sk_...
WORKOS_CLIENT_ID=client_...
BASE_URL=http://localhost:3001

# Dev only: skip auth and use this user ID
DEV_USER_ID=1
